const Daily = async ({children}: {children: React.ReactNode}) => {


    return (
        <div className={"w-[142.8rem] h-screen justify-center"}>
            {children}
        </div>
    )
}

export default Daily